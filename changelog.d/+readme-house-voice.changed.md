The README is restyled into the house voice, which changes how every reader meets the
crate. The five tables become bulleted lists with bolded lead terms, every paragraph is
held to three sentences, headings are labels rather than arguments, and bold is confined
to the one job it keeps. Three sections that did not exist are now there: a contents
list, where a bug or a vulnerability should be reported, and who should be using
`regex-automata` or `gist` instead of this crate. No claim, number, citation, or path
changed - the restyle was checked token by token against the text it replaced.
