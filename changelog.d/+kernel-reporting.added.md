shuffle::kernel() reports which of NEON, SSSE3 or scalar dispatch actually chose, and dispatch itself now reads it, so the two cannot disagree. The vector/scalar differential test asserts a vector kernel really ran — previously, a target that fell back to scalar would have compared the reference against itself and passed vacuously. Both vector paths are now exercised: NEON natively, SSSE3 under Rosetta and natively on x86_64 Linux.
