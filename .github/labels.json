{
  "_comment": [
    "The label taxonomy, and the only place it is written down.",
    "",
    "── Namespaces are the sort order",
    "GitHub lists labels alphabetically and offers no way to group them, so the",
    "grouping is spelled into the names: `area/` what it touches, `size/` how much",
    "of it moved, `type/` what kind of change it is, `status/` what a human",
    "decided, `topic/` what this crate in particular argues about.",
    "Every dropdown, filter, and pull request row therefore arrives",
    "pre-grouped, and a label's family is legible from a glance at its color: teal",
    "areas, a neutral size ramp, semantic types, hot statuses. There is no `lang/`",
    "namespace here — this crate is Rust all the way down, so the answer would be",
    "the same on every pull request and tell a reviewer nothing.",
    "",
    "── A row says how it is earned",
    "`paths` is matched against the pull request's changed files, `commit` against",
    "the conventional-commit type in its title, `from` against the count of",
    "changed lines. A row with none of those three is applied by hand and the",
    "automation will never touch it — that is the whole ownership boundary, and",
    "`.github/scripts/triage.py` reads it here rather than restating it.",
    "",
    "── Glob semantics",
    "The ones .gitignore already taught this tree. A pattern with a `/` matches",
    "the whole path, where `*` stops at a separator and `**` spans them, so",
    "`src/arch/**` is that whole subtree while `src/*.rs` is only the sieve's",
    "own modules at the root of `src/`. A pattern without a `/` matches the",
    "file's name at any depth, so `Cargo.toml` finds the one under `fuzz/` too.",
    "",
    "── Areas overlap on purpose",
    "These are a set, not a partition. `src/arch/neon.rs` earns both `area/sieve`",
    "and `area/arch`, which is the truth: the SIMD backends are part of the sieve,",
    "and a reviewer wants to know a change landed specifically in one of them.",
    "",
    "── The bare labels are the issue vocabulary",
    "`bug`, `enhancement`, `question` and their neighbours describe an issue, not",
    "a change, so no `paths`/`commit`/`from` rule can earn one and the automation",
    "never applies them. They are declared anyway, for two reasons: a label this",
    "file does not know about is one GitHub silently drops when a template asks",
    "for it, and undeclared defaults keep GitHub's stock colors, which clash with",
    "the palette below. Living outside every declared namespace is also why",
    "`sync --prune` cannot reach them.",
    "`topic/soundness` is namespaced rather than bare because it is the one report",
    "this crate cannot afford to lose in a pile of `bug`, and `good first issue`",
    "and `help wanted` keep their exact spelling because GitHub's contributor",
    "discovery looks for it.",
    "",
    "── Colors have an origin",
    "Each hex is a documented OKLCH coordinate rather than a taste: areas are",
    "oklch(0.62 0.10 195), the size ramp holds hue 250 at chroma 0.015 and walks",
    "lightness 0.90 → 0.44, and the types carry the conventional semantics (green",
    "feature, red fix, gold performance).",
    "`topic/` is oklch(0.60 0.13 330). The issue vocabulary is paired with the",
    "change types it answers to rather than given a family of its own: `bug` is",
    "a deeper `type/fix` at oklch(0.55 0.18 22), `enhancement` a lighter",
    "`type/feat` at oklch(0.78 0.10 150), and the triage outcomes sit on the",
    "size ramp's neutral hue so a closed-without-a-change reads as quiet.",
    "",
    "── The type list is a documented contract",
    "These `commit` values are the prefixes CONTRIBUTING.md tells contributors to",
    "use, and the triage check holds pull request titles to exactly that list, so",
    "the document and the gate cannot drift apart. Dependabot is configured with",
    "matching prefixes for the same reason."
  ],

  "unweighted": [
    "*.lock",
    "CHANGELOG.md",
    "fuzz/corpus/**",
    "fuzz/artifacts/**"
  ],

  "labels": [
    { "name": "area/arch", "color": "1d9999", "description": "The SIMD backends: NEON and SSSE3", "paths": ["src/arch/**"] },
    { "name": "area/build", "color": "1d9999", "description": "Cargo manifests, the pinned toolchain, and lint configuration", "paths": ["Cargo.toml", "Cargo.lock", "rust-toolchain.toml", "clippy.toml", "deny.toml", "rustfmt.toml", "towncrier.toml"] },
    { "name": "area/ci", "color": "1d9999", "description": "Workflows, the triage taxonomy, and repository automation", "paths": [".github/**"] },
    { "name": "area/docs", "color": "1d9999", "description": "README, the paper trail, and changelog fragments", "paths": ["*.md", "changelog.d/**", "LICENSE", "NOTICE"] },
    { "name": "area/fuzz", "color": "1d9999", "description": "Fuzz targets, corpora, and the differential harness", "paths": ["fuzz/**"] },
    { "name": "area/price", "color": "1d9999", "description": "Calibration, gates, and minted sieves", "paths": ["src/price/**"] },
    { "name": "area/sieve", "color": "1d9999", "description": "The sieve core: lattice, projection, shuffle, skip, selectivity", "paths": ["src/**"] },
    { "name": "area/tests", "color": "1d9999", "description": "Integration tests and examples", "paths": ["tests/**", "examples/**"] },

    { "name": "size/XS", "color": "d7dfe8", "description": "Up to 10 weighted lines", "from": 0 },
    { "name": "size/S", "color": "b7bfc7", "description": "11 to 50 weighted lines", "from": 11 },
    { "name": "size/M", "color": "9299a1", "description": "51 to 200 weighted lines", "from": 51 },
    { "name": "size/L", "color": "6e757d", "description": "201 to 600 weighted lines", "from": 201 },
    { "name": "size/XL", "color": "4c535b", "description": "Over 600 weighted lines — consider splitting", "from": 601 },

    { "name": "status/blocked", "color": "cc3336", "description": "Waiting on something outside this pull request" },
    { "name": "status/needs-triage", "color": "e8aa4d", "description": "Not yet read by a maintainer" },

    { "name": "topic/soundness", "color": "ab61a5", "description": "A sieve rejected a document the regex would have matched" },

    { "name": "type/build", "color": "9aa381", "description": "Build system, toolchain, or packaging", "commit": "build" },
    { "name": "type/chore", "color": "9ca6b1", "description": "Housekeeping with no user-visible effect", "commit": "chore" },
    { "name": "type/ci", "color": "978c62", "description": "Continuous integration and repository automation", "commit": "ci" },
    { "name": "type/deps", "color": "c8997b", "description": "Dependency updates", "commit": "deps" },
    { "name": "type/docs", "color": "369dd1", "description": "Documentation only", "commit": "docs" },
    { "name": "type/feat", "color": "4db155", "description": "A new capability", "commit": "feat" },
    { "name": "type/fix", "color": "db4241", "description": "A bug fix", "commit": "fix" },
    { "name": "type/perf", "color": "c28f00", "description": "Faster or smaller, same answer", "commit": "perf" },
    { "name": "type/refactor", "color": "9e6ec0", "description": "Same behavior, better shape", "commit": "refactor" },
    { "name": "type/revert", "color": "bc3f53", "description": "Undoes a previous change", "commit": "revert" },
    { "name": "type/style", "color": "b4bfca", "description": "Formatting only", "commit": "style" },
    { "name": "type/test", "color": "50b39b", "description": "Tests and fixtures", "commit": "test" },

    { "name": "bug", "color": "c5353e", "description": "Something is wrong — `type/fix` is the pull request that closes it" },
    { "name": "duplicate", "color": "b9bec4", "description": "Already tracked somewhere else" },
    { "name": "enhancement", "color": "88ca95", "description": "A request for something new — `type/feat` is the pull request that lands it" },
    { "name": "good first issue", "color": "54c398", "description": "Small, self-contained, and a good place to start" },
    { "name": "help wanted", "color": "74a5f5", "description": "Wanted, and nobody is on it" },
    { "name": "question", "color": "a486d7", "description": "A question about use, not a defect" },
    { "name": "wontfix", "color": "6d7277", "description": "Understood, and deliberately not changing" }
  ]
}
